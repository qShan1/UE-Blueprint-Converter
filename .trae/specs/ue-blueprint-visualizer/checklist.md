# Checklist

## Web API 后端
- [x] FastAPI 服务启动正常，CORS 配置正确
- [x] `POST /api/parse` 接口返回正确的节点/引脚/连接 JSON
- [x] `POST /api/format` 接口支持 plain/markdown/mermaid 格式
- [x] `GET /api/examples` 返回 3 个示例列表
- [x] `GET /api/examples/<name>` 返回对应示例文本内容
- [x] API 错误处理完善（空输入、非法格式等返回友好错误信息）

## 前端项目脚手架
- [x] Vite + React + TypeScript 项目搭建完成，可正常启动
- [x] 基础页面布局：顶部工具栏、左侧输入面板、右侧可视画面板
- [x] API 客户端模块正确封装所有后端接口
- [x] 开发代理配置正确，前后端联调无障碍

## 节点图渲染引擎
- [x] 节点卡片正确绘制：圆角矩形、标题栏、引脚插座
- [x] 引脚视觉区分：执行引脚用三角形/箭头标记，数据引脚用彩色圆点
- [x] 数据引脚颜色按类型区分（布尔=红、整型=青、浮点=黄、字符串=绿、对象=蓝）
- [x] 执行流连接线绘制为白色实线 Bezier 曲线
- [x] 数据流连接线绘制为彩色虚线 Bezier 曲线
- [x] 自动布局算法能按执行流顺序排列节点
- [x] 暗色主题渲染正常
- [x] 未知节点渲染为灰色卡片并标注 `[Unknown]`

## 交互操作
- [x] 节点可拖拽移动，连接线实时更新
- [x] 节点点击可选中，选中时边框高亮
- [x] 画布可平移（拖拽空白区域）
- [x] 画布可缩放（鼠标滚轮）

## 输入面板与视图切换
- [x] 蓝图文本输入区域可正常输入和粘贴
- [x] "渲染"按钮触发解析和可视化渲染
- [x] 示例加载下拉菜单可加载 3 个示例
- [x] 视图模式可在可视化 / 纯文本 / Markdown / Mermaid 之间切换
- [x] Mermaid 视图提供一键复制功能
- [x] 空状态提示"未检测到蓝图节点"

## 集成测试
- [x] 3 个示例蓝图端到端测试通过
- [x] 四种视图模式输出均正确
- [x] 交互操作流畅无卡顿
- [x] 错误输入处理正常（显示友好错误信息）
- [x] 前端单元测试覆盖 API 客户端和渲染核心逻辑